public Boolean isBirthday(Contact c) {
    //code here

    String dateOfBirth = c.Birthdate.format();
    String today = Date.today().format();
    if(dateOfBirth == today){
        return true;
    }

    return false;
}