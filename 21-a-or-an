public String aOrAn(String word) {
    //code here
    String result = '';
    if(word.startsWith(a) || word.startsWith(a) || word.startsWith(a) || word.startsWith(a) || word.startsWith(a)){
        result = 'an ' + word;
    } else
        result = 'a ' + word;

    return result;
}